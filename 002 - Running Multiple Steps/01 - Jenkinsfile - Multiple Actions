def writeToFile(text = ''){

	touch "$text"
	"echo date > $text"
	"cat $text"
}

pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
    	stage("ONE"){
		steps{
			writeToFile('a.txt')
		}
	}
    }
}
