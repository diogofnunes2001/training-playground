def writeToFile(text = ''){

	touch "$text"
	"date > $text"
	"cat $text"
}

pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
    	stage("ONE"){
		steps{
			writeToFile('a.txt')
		}
	}
    }
}
