def writeToFile(text = ''){
	sh "date > $test && cat $text"
}

pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
    	stage("ONE"){
		steps{
			writeToFile('a.txt')
		}
	}
    }
}
